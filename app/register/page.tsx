"use client";
import { useState } from "react";
import Link from "next/link";
export default function Register(){const [error,setError]=useState("");const [done,setDone]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});if(!r.ok){setError((await r.json()).error||"Unable to register");return;}setDone(true);}return <section className="page-pad section-pad"><div className="mx-auto max-w-md"><p className="text-xs uppercase tracking-[.3em] text-muted">Account</p><h1 className="display mt-4 text-5xl font-semibold">Create account</h1>{done?<div className="mt-10 border border-line p-6 text-sm">Account created. <Link href="/login" className="underline">Sign in</Link>.</div>:<form onSubmit={submit} className="mt-10 space-y-6"><input name="name" required placeholder="Name" className="w-full border-b border-line py-3 outline-none"/><input name="email" type="email" required placeholder="Email" className="w-full border-b border-line py-3 outline-none"/><input name="password" type="password" minLength={8} required placeholder="Password (8+ characters)" className="w-full border-b border-line py-3 outline-none"/>{error&&<p className="text-sm text-sale">{error}</p>}<button className="w-full bg-black py-4 text-sm font-medium text-white">Create account</button></form>}</div></section>}
